<h1>Games</h1>

<table class='table'>
  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.name %></td>
        <% if game.current_player %>
          <td>current player: <%= game.current_player.name %></td>
          <td>total players: <%= game.players.count %></td>
        <% else %>
          <td>Game has no players yet</td>
          <td/>
        <% end %>

        <% if active_words_count(game) > 0 %>
          <td><%= active_words_count(game) %> words available. <%= link_to 'Play', play_game_path(game) %></td>
        <% else %>
          <td>Assign words first </td>
        <% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game', new_game_path %>
